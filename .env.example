# Home Instead Portal Credentials
SITE_URL=https://portal.uniqueiq.co.uk/Account/Login?ReturnUrl=%2f
USERNAME=your-email@example.com
PASSWORD=your-password

# Email configuration (comma-separated list for multiple recipients)
EMAIL_TO=recipient1@example.com, recipient2@example.com
EMAIL_FROM=sender@example.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Coverage gap detection (optional - defaults shown)
# COVERAGE_START_HOUR=9          # Start of coverage period (24-hour format)
# COVERAGE_END_HOUR=18           # End of coverage period (24-hour format)
# COVERAGE_MAX_GAP_MINUTES=120   # Maximum allowed gap in minutes (alert if exceeded)

# Missing timesheet alert (optional - defaults shown)
# MISSING_DATA_ALERT_DAYS=4      # Alert if no timesheet data within this many days
